EvidenceFinder.Filter = function(settings) {
    var settings = settings || {};
    this.id = settings.id;
    this.label = settings.id;
    this.filterType = settings.filterType;
};

EvidenceFinder.Filter.prototype = {
};
